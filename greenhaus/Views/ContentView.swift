import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationView {
            VStack {
                BackgroundImage()
            }
            .navigationTitle("Greenhaus")
        }
        
    }
}

#Preview {
    ContentView()
}
